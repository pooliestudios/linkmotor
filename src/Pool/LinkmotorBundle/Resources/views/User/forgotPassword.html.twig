{% extends 'PoolLinkmotorBundle::login.html.twig' %}

{% block content %}
<div class="uk-panel uk-width-1-3 uk-container-center">
			<p class="uk-text-center">
	            <a href="{{ path('pool_linkmotor_index') }}">
	                <img src="{{ asset('bundles/poollinkmotor/img/logo.png') }}" width="180" class="logo" alt="Linkmotor">
	            </a>
			</p>

<div id="loginbox">
    <h2>{% trans %}Did you forget your password?{% endtrans %}</h2>

    <form class="uk-form" action="{{ path('pool_linkmotor_forgot_password') }}" method="post">
        <fieldset>
            {{ form_widget(form) }}
        </fieldset>

        <button type="submit" class="uk-button uk-button-primary">{% trans %}Sent password-reset e-mail{% endtrans %}</button>
        <small><a class="lmcancelbutton" href="{{ path('login') }}">{% trans %}Cancel{% endtrans %}</a></small>
    </form>
    </div>

</div>
{% endblock %}
